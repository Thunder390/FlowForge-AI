{
  "name": "@flowforge/renderers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "FFIR to human-facing artifacts: mermaid, setup guide, integrations list, canvas data. Siblings of the compiler, not targets of it.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flowforge/ffir": "workspace:*",
    "@flowforge/registry": "workspace:*"
  },
  "devDependencies": {
    "@flowforge/config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.8"
  }
}
